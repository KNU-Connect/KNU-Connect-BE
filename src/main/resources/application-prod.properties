# Production Environment Configuration

# Server
server.port=8080

# Database (MySQL/PostgreSQL for production)
# TODO: Update with actual production database credentials
#spring.datasource.url=jdbc:mysql://localhost:3306/knuconnect?useSSL=false&serverTimezone=Asia/Seoul
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#spring.datasource.username=${DB_USERNAME}
#spring.datasource.password=${DB_PASSWORD}

# JPA
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.show-sql=false

# Logging
logging.level.root=WARN
logging.level.com.example.knu_connect=INFO
logging.level.org.hibernate.SQL=WARN

# Swagger (disable in production for security)
springdoc.swagger-ui.enabled=false
springdoc.api-docs.enabled=false
